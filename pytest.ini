[pytest]
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Disable problematic ROS plugins
addopts = 
    -v
    --tb=short
    -p no:launch_testing

norecursedirs = 
    .git
    .tox
    dist
    build
    *.egg
